{
  "tests": [
    {
      "name": "Access denied without token",
      "method": "GET",
      "expectedStatus": 401,
      "expectedBody": {
        "error": "Authentication token required"
      },
      "bodyMatcher": "partial"
    }
  ]
}