{
  "tests": [
    {
      "name": "Access denied without admin role",
      "method": "GET",
      "expectedStatus": 401,
      "expectedBody": {
        "error": "Authentication token required"
      },
      "bodyMatcher": "partial"
    }
  ]
}